#Xiaofan Wu 903152422

adopt  =  Cat(userin("Congrats! What's your new cat's name?"), userin("What color?"), userin("What's its favorite food?"))
name = userin("What's the name of the cat you wanna select?")
<cat> = cat_dict[name]
feed <cat>  =  <cat>.feed(userin("Feed what food?"))
digest <cat> = <cat>.digest()
jump <cat> = <cat>.jump(int(userin("How many time do you want {} to jump?".format( <cat> .name ))))
pat <cat> = <cat>.pat(userin("Which body part do you want to pat?"))
census  =  <cat> … ##for all cats that exist
quit = <cat> … ##for all cats that exist